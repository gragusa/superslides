---
title: "Superslides Template"
subtitle: "Super features demo"
authors:
  - name: "Giuseppe Ragusa"
    affiliation: "Luiss University"
date: "2025-09-26"
lecture-date: "September 26, 2025"

editor:
  render-on-save: true
# Note: This document uses the _quarto.yml configuration
# You can override any setting here if needed

# Example of overriding some settings for this specific document
format:
  superslides-typst:
    keep-typ: true
    #===================================================================
    # TYPOGRAPHY SETTINGS
    #===================================================================
    fontsize: 24pt                    # Base font size
    mainfont: "Inter"                # Body text font
    sansfont: "Inter"                # Heading font
    font-weight-heading: "regular"    # Heading weight: light, regular, bold
    font-weight-body: "regular"       # Body weight: light, regular, bold
    # handout: true                   # Generate handout version (removes animations)

    # Title slide typography
    title-font: "Inter"              # Title font family
    title-size: 42pt                  # Title font size
    title-weight: "bold"              # Title font weight
    # font-size-title: 2.0em          # Alternative title size (relative to base)
    # font-weight-title: "bold"       # Alternative title weight
    subtitle-font: "Inter"           # Subtitle font family
    subtitle-size: 30pt               # Subtitle font size
    subtitle-weight: "regular"        # Subtitle font weight
    # font-size-subtitle: 1.2em       # Alternative subtitle size (relative to base)
    author-size: 20pt                 # Author font size
    date-size: 18pt                   # Date font size

    #===================================================================
    # COLOR SCHEME (3-COLOR SYSTEM)
    #===================================================================
    jet: "#404040"                  # Main text color (dark blue-gray)
    accent: "#2836A6"               # Primary accent (sea green) - headings, links, buttons
    accent2: "#004494"              # Secondary accent (peru) - highlights, warnings

    # Author styling colors
    affiliation-color: "#707070"    # Affiliation text color
    affiliation-style: "italic"       # Affiliation style: normal, italic
    # affiliation-weight: "regular"   # Affiliation text weight: light, regular, bold
    email-color: "#CD853F"          # Email link color

    #===================================================================
    # CODE BLOCK CONFIGURATION
    #===================================================================
    # Standard code blocks
    raw-font-size: 14pt               # Code block font size
    raw-inline-size: 22pt             # Inline code font size
    raw-inset: 8pt
                        # Code block padding (controls line spacing!)
      

    #===================================================================
    # LIST CUSTOMIZATION
    #===================================================================
    list-indent: 0.6em                  # List indentation
    list-marker-1: "•"                # Level 1 bullet
    list-marker-2: "◦"                # Level 2 bullet
    list-marker-3: "▪"                # Level 3 bullet

    #===================================================================
    # INTERACTIVE ELEMENTS
    #===================================================================
    # QR Code
    qr-code-url: "https://github.com/gragusa/superslides"
    qr-code-title: "View Source"      # Button text
    qr-code-size: 4cm                 # QR code size (can use cm, pt, em)
    qr-code-button-color: "#404040"   # Button color

    # Updates functionality
    updates-link: "https://github.com/gragusa/superslides/releases"
    # last-updated-text: "Last updated:"  # Custom text for last updated label

    #===================================================================
    # LAYOUT & BRANDING
    #===================================================================
    # Background
    # background-color: "#FFFFFF"     # Slide background color
    # background-image: "bg.png"      # Background image path

    # Title page customization
    # logo-path: "logo.png"           # Organization logo
    title-compact: false              # Compact title layout

    #===================================================================
    # LANGUAGE & LOCALIZATION
    #===================================================================
    lang: "en"                        # Interface language: "en" or "it"

    #===================================================================
    # ADVANCED CUSTOMIZATION
    #===================================================================
    # Box colors (for callouts/alerts)
    # simplebox-color: "#EEF4FF"      # Simple box background
    #   Info light bg: #EEF4FF (very light tint of your accents)
    #   Info border: #B9C9F5
    #   Info text / icon: #1F2E85
    # warningbox-color: "#FFF6E0"     # Warning box background
    #   Warning border: #E8D2A8
    #.  Warning text / icon: #7A5A00
    # infobox-color: "#D1ECF1"        # Info box background
    #.    


    # Alert/strong text color
    alert-color: "#FF6B35"            # Color for bold/strong text and alerts
---

# Getting Started

## Introduction

Welcome to **Superslides**! This template demonstrates:

- Enhanced Typst presentations with Touying
- Professional title slides with interactive elements
- Advanced code blocks with zebraw integration
- Complete customization through YAML parameters

## Features Overview

**Key capabilities:**

- 🎨 Professional title slides with left-aligned layout
- 🔗 Interactive QR codes and clickable links
- 📝 Enhanced code blocks with mathematical annotations
- 🎯 40+ YAML parameters for customization
- 🌐 Multi-language support (English/Italian)

## Typography

Standard markdown formatting works as expected:

  - _emphasis_ (`_emphasis_`)
  - **bold** (`**bold**`)
  - **_bold emphasis_** (`**_bold emphasis_**`)
  - ~~strikethrough~~ (`~~strikethrough~~`)
  - [alert]{.alert} (`[alert]{.alert}`)
  - [strong]{.strong} (`[strong]{.strong}`)

## Code Blocks

Superslides supports standard code blocks with proper syntax highlighting and customizable formatting.

```{r}
plot(rnorm(10))
```
## Code Block Configuration

You can control code block appearance through YAML parameters:

- `raw-font-size: 20pt` - Font size for code blocks
- `raw-inline-size: 20pt` - Font size for inline code
- `raw-inset: 2pt` - Padding around code blocks

## Python Example

```python
def quadratic_formula(a, b, c):
    """Solve quadratic equation ax² + bx + c = 0"""
    discriminant = b**2 - 4*a*c

    if discriminant < 0:
        return None  # No real solutions
    elif discriminant == 0:
        return -b / (2*a)  # One solution
    else:
        sqrt_d = discriminant**0.5
        x1 = (-b + sqrt_d) / (2*a)
        x2 = (-b - sqrt_d) / (2*a)
        return x1, x2
```

## R Statistical Analysis

```r
calculate_statistics <- function(data) {
    n <- length(data)
    mean_x <- sum(data) / n
    variance <- sum((data - mean_x)^2) / (n-1)
    std_dev <- sqrt(variance)
    se <- std_dev / sqrt(n)

    return(list(
        mean = mean_x,
        variance = variance,
        std_dev = std_dev,
        std_error = se
    ))
}
```

## Julia Mathematical Functions

```julia
function fibonacci(n)
    if n <= 1
        return n
    else
        return fibonacci(n-1) + fibonacci(n-2)
    end
end
```

## Inline Code

You can also use `inline code` within text. The font size is controlled by the `raw-inline-size` parameter.

## More Information

Learn more about creating custom Typst templates:

<https://quarto.org/docs/prerelease/1.4/typst.html#custom-formats>
